# Recurly Signature Generator API 
Small Sinatra(link) application that generates signatures and exposes a single API endpoint for fetching them.

## TL;DR
1. Grab the code and `bundle` the gems.
2. `unicorn`
